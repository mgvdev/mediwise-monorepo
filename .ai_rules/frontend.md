# Frontend Rules

Generated by `ai-init`. This file is managed by tooling.

- Category: `frontend`
- Generated at: `2026-06-30T15:45:04.500Z`

## Component pattern

How components are structured.

- **Atomic design**: Organize UI as atoms, molecules, organisms, templates, and pages.
- **Compound components**: Build complex widgets as compound components with shared context APIs.

### Rationale

- These selections define default implementation constraints for this category.
- Deviations should be explicitly documented when needed.

## Styling strategy

How styles are authored and maintained.

- **Utility-first**: Use utility-first classes and limit custom CSS to tokens and exceptions.

### Rationale

- These selections define default implementation constraints for this category.
- Deviations should be explicitly documented when needed.

## State strategy

How client state and server state are handled.

- **Query cache**: Use server-state caching library for remote data and keep local UI state minimal.
- **Custom instruction**: use ReactQuery with cache

### Rationale

- These selections define default implementation constraints for this category.
- Deviations should be explicitly documented when needed.

## UI library packaging

How reusable components are packaged and documented.

- **App-local components**: Keep components in the application repo and avoid publishing a separate UI package.
- **Separate package + Storybook**: Maintain reusable UI components in a dedicated package and document them in Storybook.
- **Custom instruction**: Separate pacakge for web (packages/medi-ui) and app-local components for mobile app

### Rationale

- These selections define default implementation constraints for this category.
- Deviations should be explicitly documented when needed.

## Component library standard

Operational standards for the component library stack.

- **Library-agnostic**: Do not enforce a specific component library standard; apply generic design-system and accessibility conventions instead.

### Rationale

- These selections define default implementation constraints for this category.
- Deviations should be explicitly documented when needed.
